import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const header = () => {
  return (
    <View>
      <Text style={{backgroundColor:'red'}}>header</Text>
    </View>
  )
}

export default header

const styles = StyleSheet.create({})